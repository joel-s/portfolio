gnuplot <<_EOF
# GNUPLOT CMD FILE FOR PLOTTING HISTOGRAM DATA AS A PS
# Input line fmt is "count value"
set terminal postscript
set output "$1.ps"

set title "Title"
set nokey
set grid
set xrange [0:]
set xlabel "X Axis"
set ylabel "Y Axis"
plot "$1" using 2:1 with points
_EOF

# grep "bytes took" /var/opt/SUNWSMS/adm/platform/messages |awk '{split($16,a,"m");printf("%s, %s\n",$13,a[1])}' > /var/tmp/plotNEW
# ksh ~/bin/plotdata plotNEW
